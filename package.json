{
  "name": "agios-trading-workspace",
  "version": "1.0.0",
  "description": "Workspace for Trading 2.0",
  "scripts": {
    "prestart": "npm install --prefix backend && npm install --prefix frontend && pip install -r lstm_network/requirements.txt --quiet",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:lstm\"",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "start:lstm": "cd lstm_network && python3 app.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
